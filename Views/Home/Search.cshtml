@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@model IEnumerable<MovieModel>

<h2 style="color: white ; font-family: fantasy">Movies</h2>
@if (Model == null)
{
    <p>@ViewData["userInput"] not found. Please try again.</p> @*how can I put the movie title not found?*@
}
else
{

<table class="table" height="100px" style="border-radius: 25px;">
    <tr>
        <th style="color: white">Title</th>
        <th style="color: white">Year</th>
        <th style="color: white">Poster</th>
    </tr>

    @foreach (var movie in Model)
    {
        <tr height="100px">
            <td style="color: white">@Html.ActionLink(@movie.Title, "ViewMovie", new { movieTitle = @movie.Title, imdbID = @movie.imdbID })</td>
            <td style="color: white">@movie.Year</td>
            <td><div><img src="@movie.Poster" /></div></td>
        </tr>
    }
</table>
}